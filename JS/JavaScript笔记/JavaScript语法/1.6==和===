一、等于操作符==
    (在比较中会先进行类型转换，再确定操作数是否相等)
    1、两个都为简单类型，字符串和布尔值都会转换成数值，再比较
    2、简单类型与引用类型比较，对象转化成其原始类型的值，再比较
    3、两个都为引用类型，则比较它们是否指向同一个对象
    4、null 和 undefined 相等（null == undefined）
    5、存在 NaN 则返回 false (NaN这个特殊的Number与所有其他值都不相等,包括它自己,唯一能判断NaN的方法是通过isNaN(NaN)函数)
        并且强制类型转换还会带来性能消耗。
二、全等操作符===
    (不会自动转换数据类型,如果数据类型不一致返回false,如果一致再比较值)undefined 和 null 与自身严格相等
    当其中的操作数为对象时，只有指向同一个对象才返回true
三、区别
    相等操作符(==)会做类型转换，再进行值的比较，全等运算符不会做类型转换；null 和 undefined 比较，相等操作符(==)为true，全等操作符(===)为false
除了在比较对象属性为null或者undefined的情况下，我们可以使用相等操作符(==)，其他情况建议一律使用全等操作符(===)
四、isNaN()这个函数，只能判断参数 不是数字就返回true
    isNaN(NaN); // true
    isNaN('ddd'); // true
    isNaN(123); // false
    isNaN('123'); // false
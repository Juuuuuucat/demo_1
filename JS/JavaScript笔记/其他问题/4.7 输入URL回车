在浏览器中输入URL回车后，
  1、浏览器解析URL的构成，操作系统会先检查自己本地的hosts文件中是否有这个网址映射关系，如果有就调用这个IP地址进行域名解析；没有就查找本地域名服务器缓存，有就返回，没有就向上级域名服务器进行迭代查询，本地域名服务器会将得到的IP地址缓存起来，然后返回给客户端，
  2、客户端发起TCP连接请求，进行三次握手，然后发送HTTP请求
  3、服务器响应请求，返回数据或其他错误信息
  4、浏览器解析得到的HTML代码，进行页面渲染
    处理HTML标签并构建DOM树
    处理CSS并构建CSS规则树（和上面互不影响）
    当遇到<script>标签时，一旦发现有对JavaScript的引用，就会立即下载脚本，阻断文档的解析，等脚本执行完成后，再开始文档的解析
    将生成完毕的DOM树和CSS规则树合并成一个渲染树
    根据渲染树来布局，计算每个节点的位置和尺寸
    最后调用系统渲染页面
  5、HTTP请求结束后断开TCP连接，四次挥手